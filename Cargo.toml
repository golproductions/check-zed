[package]
name = "check-zed"
version = "1.0.0"
edition = "2021"
description = "GOL Check extension for Zed — anti-hallucination firewall"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
